{% extends 'base.html.twig' %}

{% block title %}Categorie {{ name }} {% endblock %}

{% block body %}
    {# <h1>Nos articles</h1>

    <div class="container">

          <div class="row">
             {% for blog_post in blog_posts %}
                <div class="col-md-4">
                <div class="card mb-4 box-shadow">
                    <img class="card-img-top" src={{ asset(blog_post.image.url) }} alt="Card image cap">
                    <div class="card-body">
                        <h4>{{ blog_post.title }}</h4>
                        <p class="card-text">{{ blog_post.description | striptags | slice(0, 100) }} [...]</p>
                        <div class="d-flex justify-content-between align-items-center">
                        
                            <a class="btn btn-primary" href="{{ path('blog_post_show', {'id': blog_post.id}) }}">View</a>
                        
                        
                            <small class="text-muted">{{ blog_post.createdAt|date('d-m-Y') }} à {{ blog_post.createdAt|date('H:i') }}</small>
                        </div>
                    </div>
                </div>
            </div>
            {% else %}
            <h3>Aucune article</h3>
            {% endfor %}
            {{ knp_pagination_render(blog_posts, '@KnpPaginator/Pagination/twitter_bootstrap_v3_pagination.html.twig') }}
        </div> #}
        <div id="root"></div>

    
{% endblock %}
